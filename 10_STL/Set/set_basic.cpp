/*A set is a container that stores only unique 
elements in sorted (ascending) order. Duplicate element are remove by itself*/
#include <bits/stdc++.h>
using namespace std;

int main() {
 set <string> name ;
 //Function for give input in set
 name.insert("OG Panther");
 name.insert("Ruhul Amin");
 name.insert("Vishnu kant rai");
 name.insert("OG yugesh");
 name.insert("Aniket kumar singh");
 return 0;
}